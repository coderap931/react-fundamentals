const uername = 'Kenn';
console.log(`${username} is the true JS Wizard!`);

console.log(`Hey students,
What bugs can
${username}
fix
for you today?`);

const person = {
    username: 'Kenn',
    profession: 'Secret Agent'
};

console.log(`${persone.username}, ${person.profession}, extraordianaire`); // Kenn, Secret Agent, extraordinaire